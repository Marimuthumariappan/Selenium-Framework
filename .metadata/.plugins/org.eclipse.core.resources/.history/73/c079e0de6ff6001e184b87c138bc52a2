package com.test;

import org.testng.annotations.AfterTest;
import org.testng.annotations.BeforeTest;
import org.testng.annotations.Test;

import com.base.Base;
import com.base.Browser;
import com.steps.HomepageSteps;

public class TC001 	extends Base {
	private HomepageSteps homepageSteps;

    @BeforeTest
    public void setupTest() {
        browseropen("https://www.orangehrm.com/");
        homepageSteps = new HomepageSteps();  // Initialize HomepageSteps object
    }

    @Test
    public void HeaderVerify() throws Exception {
        homepageSteps.displayedOnHeaders();
    }

    @AfterTest
    public void tearDownTest() {
        quit();  // Quit the WebDriver session after the test
    }
	
}
